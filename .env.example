# ── MCP Server (apps/mcp-server) ─────────────────────────────────────────────
MCP_BASE_URL=https://your-domain.com/rejeki/mcp
INTROSPECT_URL=http://127.0.0.1:9004/introspect
PORT=8001
# TEST_TOKEN=any-value     # dev only: aktifkan TestTokenVerifier
# TEST_DB=./users/test.db  # dev only: path ke SQLite lokal

# ── Auth Server (apps/auth-server) ───────────────────────────────────────────
AS_BASE_URL=https://your-domain.com/rejeki/auth
AUTH_PORT=9004
USERS_CONFIG=/absolute/path/to/users.json
